package org.example.browser.shop;

import java.util.List;

public class ShopListSelector {
    public static void beginInterfacingWithGame(List<String> shopList) {
        // for (String line : shopList) {
        //     if (PageStrings.BUY.containedIn(line)) {
        //         // TODO: map line to matching <button> (CDP click / label match)
        //     }
        // }
    }
}